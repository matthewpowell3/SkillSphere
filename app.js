// add the button in javscript
const button = document.getElementById('submit-button').classList;

// adding classlist you have to use classlist
function animation() {
  setInterval(button.add('animationButton'), 3000);
}
